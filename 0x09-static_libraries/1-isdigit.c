#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isdigit - function check if c is digit or not
 *
 * @c: argument to be checked
 *
 * Return: 0 if numeric charachter, otherwise if not
 */

int _isdigit(int c)
{
	return (isdigit(c));
}

